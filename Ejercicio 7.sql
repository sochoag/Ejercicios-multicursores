-- Ejercicio 7

SELECT
  user_id,
  name,
  email
FROM users;

-- Resultado esperado:

SELECT
  user_id,  -- Campo de la tabla
  name,     -- Campo de la tabla
  email     -- Campo de la tabla
FROM users;